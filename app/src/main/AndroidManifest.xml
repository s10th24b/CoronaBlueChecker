<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.s10th24b.app.coronabluechecker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoronaBlueChecker"
        android:usesCleartextTraffic="true">
        <activity android:name=".CheckWebViewActivity"></activity>
        <activity android:name=".UsageActivity" />
        <activity android:name=".WebViewPracticeActivity" />
        <activity android:name=".MapBoxPracticeActivity" />
        <activity android:name=".PersonalVisualization" />
        <activity android:name=".PublicVisualization" />
        <activity android:name=".VisualizationMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>